# =============================================================================
# 视频AI智能识别及预警管理信息系统 - 火焰识别
# Python依赖包
# =============================================================================

# Web框架
Flask==3.1.0
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3

# JWT认证（与王永林后端API对接）
PyJWT==2.9.0

# 密码哈希
Werkzeug==3.1.3

# 数据库
SQLAlchemy==2.0.35

# 文档处理（python-docx 用于读取文档）
python-docx==1.2.0

# HTTP请求（用于与外部API对接）
requests==2.32.3

# 跨域支持
Flask-CORS==5.0.0

# 生产服务器
gunicorn==23.0.0

# =========================================================================
# 边缘AI模块依赖（人员A — 郭俊奇）
# =========================================================================
# ONNX Runtime CPU推理（Windows/Linux通用）
onnxruntime>=1.15.0
# 计算机视觉
opencv-python>=4.8.0
numpy>=1.24.0,<2.0

# 以下为Orange Pi 5 NPU加速所需（仅Linux arm64安装）:
# rknn-toolkit-lite2>=2.0.0

# 以下为模型训练所需（可选）:
# ultralytics>=8.0.0
# torch>=2.0.0
